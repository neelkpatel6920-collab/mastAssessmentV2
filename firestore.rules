rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Production: all app access goes through Next.js server APIs using Firebase Admin SDK.
    // Do not allow direct browser/client reads or writes to Firestore.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
